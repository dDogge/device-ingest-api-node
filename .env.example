ENV=dev
LOG_LEVEL=info

HOST=0.0.0.0
PORT=3000

# Auth (later)
JWT_SECRET=change-me
JWT_ISSUER=device-ingest-api-node

# Downstream services (optional)
ANALYTICS_BASE_URL=http://localhost:8000
REGISTRY_BASE_URL=http://localhost:5000

# Storage (optional early; can be in-memory first)
DATABASE_URL=sqlite://./dev.db
